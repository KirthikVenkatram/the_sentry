# Sentry

Automated Security Operations with Human-in-the-Loop using MCP Agents.

## Architecture
(Architecture Diagram Placeholder)

## Usage
1. Install dependencies: `pip install -r requirements.txt`
2. Run Server: `python -m mcp_server.server`